import Placeholder from '@/components/Placeholder';

export default function Page() {
  return (
    <Placeholder
      title="Work Plans"
      phase="Phase 2C"
      summary="Per-control tactical plans with concrete tasks, owners, success criteria, and progress notes. Defaults are seeded from a NIST CSF 2.0 work-plan template; each tenant can customize."
    />
  );
}
