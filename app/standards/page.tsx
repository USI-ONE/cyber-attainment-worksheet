import Placeholder from '@/components/Placeholder';

export default function Page() {
  return (
    <Placeholder
      title="Security Standards"
      phase="Phase 2B"
      summary="Toggle which compliance standards apply (HIPAA, ISO 27001, PCI DSS, SOC 2, etc.) and map each to the underlying NIST CSF controls. Drives compliance crosswalks on board reports."
    />
  );
}
